{
  "name": "palmergill-site",
  "version": "2.1.0",
  "description": "Personal project site with shared API-backed apps",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@vercel/functions": "^3.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
